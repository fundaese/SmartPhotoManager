<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.SmartPhotoManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/nudeRed</item>
    </style>

    <style name="Theme.SmartPhotoManager" parent="Base.Theme.SmartPhotoManager" />

    <!-- Bottom Navigation Style -->
    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="itemTextAppearanceActive">@null</item>
        <item name="itemTextAppearanceInactive">@null</item>
        <item name="itemBackground">@android:color/transparent</item>
    </style>

    <!-- Toolbar theme -->
    <style name="CustomToolbarTitleTextAppearance" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>